# 2026-06-15 푸시 기록

총 14개 커밋 (`7f2a4ce` → `345c70e`). 종결 페이지 전면 리디자인 + Ch2/Ch3 매거진 페이지 폴리시 + 매거진 article 안정성 보강이 중심.

## `7f2a4ce` — v3 copy + closing flow 개선
- **Ch3 GrowthVision Editor's Question 카피**
  - "다가오는 5년," 추가
  - "현재의 내가 아니라~ 작성해요" 부연문 삭제
  - placeholder: "어떤 사람으로 성장하고 싶은지 자유롭게 써주세요"
  - 앵커 어미: "하는 사람이 되고 싶어요" → "사람으로 성장하고 싶어요"
- **매거진 Editor's Cards 페이지**
  - 빈 하단 흡수 — 세로 가운데 정렬
  - 가치 1개 → 선택된 가치 3개 모두 표시
- **"4년 후 비전" → "5년 후 비전"** 표기 통일 (Magazine summary / stub / LLM prompt 라벨 전체)
- **Ch2 강점 stub 개선**: 가치 단어 2개 하드코딩 → `selectedValues` 전체 사용 / "~만들어내는 것." → "~만들어내고 있어요"
- **LLM 프롬프트 규칙 추가**: BEAT 4 가치 N개 모두 인용 강제 + 명사형 종결 금지
- **종결 페이지(C-5) 무한 루프 해소**: C-2b 재진입 패턴 제거 → `ClosingChoiceScene` 으로 전환, "내 매거진 펼쳐보기" 모달 + PDF 다운로드

## `9463348` — Ch3 기록 탭에 "엘아울의 발견" 추가
- Ch2 와 동일한 패턴으로 Ch3 BEAT 합성(`growthVisionSynthesis`)을 기록 탭에 표시
- 위치: "기여하고 싶은 것" 다음, "나의 성장 비전 문장" 직전
- 토픽 라벨: 내면의 부름 / 이미 시작된 움직임 / 안개를 걷어낼 도구 / 종착지의 풍경
- 포매터 리팩토링: `fmtBeatSynthesis(raw, topics)` 공통 함수로 묶고 `fmtStrengthSynthesis`·`fmtGrowthVisionSynthesis` 가 위임

## `77de381` — v3 씬 글꼴 크기 가독성 보정
- 15개 v3 씬/UI 컴포넌트의 작은 텍스트(11~13px) 일괄 상향 (대부분 14px·일부 16~18px)
- 영향 파일: Chapter2MagazineScene*, GrowthVisionSynthesisScene*, IntroScene, MagazineHandoffScene, MagazinePosterScene, StrengthSynthesisScene, ValueCardScene, ValueRankScene, ChapterReviewOverlay, MagazineArticlePage, VolumeControl
- 코드 로직 변경 없이 Tailwind `text-[Npx]` 값만 조정

## `92a39d9` — RitualScene 대화창/CTA 컨트롤 정리
- buttonLabel 명시된 ritual 씬: "다음" 힌트 숨김 + 대화창 클릭 advance 비활성, CTA 버튼만 동작
- buttonLabel 없는 ritual 씬: 대화창 클릭으로만 진행, CTA 버튼 미렌더
- 0-5-1 "네, 이해했어요" 화면의 버튼 겹침 회귀 해소

## `87db712` — PaginatedNarration 빈 "다음" 힌트 영역 정리
- 마지막 페이지 + onAdvance 미지정 시 힌트 박스 자체를 미렌더
- 노출 시에도 `pointer-events-none` 으로 CTA 버튼 클릭 가로채기 방지

## `46c65e6` — Ch2/Ch3 요약 페이지: 재질문 hint 제거 + 완성 도장 위치 조정
- **Ch2**:
  - "조금 더 OO님다운 표현을 찾아볼까요?" 재질문 hint 삭제
  - `judgeBranch` retry 메커니즘 제거 — 한 번 적은 정체성은 그대로 수락
  - 관련 state(`attempts`/`editorHint`/`judging`/`MAX_ATTEMPTS`) 제거
  - 완성 도장 `bottom-4` → Editor's Question 섹션 내부 `top-12`
- **Ch3**: 완성 도장 `top-4` → `top-12` (Editor's Question 텍스트 라인에 정렬)

## `94f7615` — 종결 페이지 리디자인 + 별첨 폰트 + Ch3 인풋 통일
- **종결 페이지(C-5)**: 상단 chrome(기록·음향) / ProgressRail(✦·Closing) / 이전 버튼 모두 숨김, 로고만 노출. 2-col 레이아웃 — 좌측 소감 textarea, 우측 매거진 펼쳐보기 + 다운받기 + 다시 플레이하기 + 경고 문구. `session.closingFeedback` 필드 신설
- **별첨(Appendix) 폰트**: 본문 답변 14→16px, 챕터 제목 16/17→18/19px, 부제 14→15px (`ChapterAttachment` / `AppendixSpread`)
- **Ch3 인풋 UI 통일**: textarea(2줄 rounded 박스) → Ch2 와 동일한 dashed-underline + 별도 input 필드 조합

## `12dec09` — 종결 페이지 폴리시 (사운드 / 버튼 / 모달)
1. **효과음 정지**: `trainLoop` 종료 인덱스를 `C-4`(미사용) → `C-5(closingChoice)` 로 교체
2. **소감 보내기 버튼**: textarea 아래 추가. 전송 후 "전송됐어요 :)" + "다시 보내기" 라벨
3. **펼쳐보기·다운받기 반복 사용**: `StoryButtonV3` 의 `pressed` 상태를 ritual 클릭 후 자동 해제
4. **모달 높이 축소**: `items-start` + `pt-[110px]` + `max-h-[calc(100vh-140px)]` — 상단 로고 겹침 해소
5. **다운받기 line 버튼**: 커스텀 outline 버튼으로 교체

## `3ecba61` — 티켓 이미지 PNG 교체 + 직무 텍스트 오버플로 회귀 수정
- 티켓 자산 `vision_ticket_new.jpg` → `/ticket.png` (alpha 보존). 검정 배경 띠 제거 — JPG 변환(`ac9c70d`, LG WAF webp/avif 차단 회피) 부작용 해소
- Journey(직무) 오버레이: 길이별 단계 font-size + `max-width: 28%` + `text-ellipsis` 안전망 + `left 62% → 60%`. "설계/엔지니어링" · "커뮤니케이션" 같은 긴 직무가 티켓 우측으로 밀리던 회귀 해결

## `2dddddc` — 종결 페이지: 소감 섹션 삭제 + 남은 두 액션을 2단 레이아웃으로 재구성
- 좌측: "나의 매거진은 언제든 다시 볼 수 있어요." + [내 매거진 펼쳐보기] + [내 매거진 다운받기(line)]
- 우측: "시간이 흘러 다시 나를 잃어버린 것 같다면…" + ※ 경고 + [다시 플레이하기]
- `md:border-l` 좌우 분리선 유지
- `feedback`/`feedbackSent` state, `persistFeedback`/`sendFeedback`, patch import 제거. `session.closingFeedback` 필드는 backward-compat 으로 `types.ts` 에 그대로 둠

## `98d6c1a` — 종결 페이지: 매거진 버튼 inline + 박스 높이 축소
- 좌측 섹션 `[펼쳐보기]` / `[다운받기]` 세로 스택 → `flex flex-wrap gap-3` 한 줄 inline
- 대화창 wrapper `min-h-[480px]` → `min-h-[280px]`, padding `py-8` → `pt-7 pb-7`

## `31e1c50` — chapter articles 빈 캐시 회귀 회복
- `session.chapterArticles[N]` 에 객체는 있는데 `headline`/`body` 가 빈 stale 캐시 → 매거진 모달·PDF 에서 빈 챕터로 노출되던 회귀 해결
- `MagazinePosterScene` missing 필터에 빈 body/headline 케이스 포함
- `MagazineHandoffScene` / `ClosingChoiceScene` 의 `articleFor` 에 `isUsable` 가드 추가 — 비어 있으면 새로 LLM 호출
- 빈 응답은 캐시 금지 → 다음 진입 시 재호출

## `811ed23` — chapter article raw markdown 노출 제거
- LLM 출력의 `**bold**` / `**PULL:**` / `[HEADLINE: ...]` 마커가 본문에 그대로 새던 회귀 fix
- `prompts.ts` 파서가 `**HEADLINE:**` 같이 bold 감싼 라벨도 캡처하도록 `\*{0,2}` 허용
- `cleanArticleField` 헬퍼 추가 — `**bold**` → bold, 짝없는 `**` 제거, 인라인 라벨 제거
- `MagazineArticlePage` / PDF data 조립 시점에서도 sanitize 적용 (캐시된 stale 데이터 호환)

## `345c70e` — 종결 배경 통일 + sanitize 모듈 client-safe 분리
- **배경 통일**: `closing.ts` 의 C-1·C-2·C-2b·C-3 모두 `morning-room.jpg` → `chapter05.jpg`. Ch4 "긴 시간 고생 많으셨어요" 의 조명 있는 객실 배경과 통일
- **빌드 오류 fix**: 직전 `811ed23` 가 client 컴포넌트에서 `prompts.ts`(server-only `node:async_hooks` 의존) 를 임포트해 Turbopack build 가 "chunking context does not support external modules" 로 실패하던 회귀 해결
- `cleanArticleField` 를 `src/lib/v3/llm/articleSanitize.ts` 로 분리 → 3개 client 컴포넌트 import 경로 갱신, `prompts.ts` 는 새 모듈에서 재import

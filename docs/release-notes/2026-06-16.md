# 2026-06-16 푸시 기록

총 8개 커밋 (`c79339d` → `b584094`). 오전 씬 단계 마무리 후 오후부터 **PDF 매거진 전면 리뉴얼** — A5 → A4, 베이크된 jpg 배경 위에 동적 텍스트 오버레이.

## `c79339d` — Ch3/Ch4 폴리시 + 씬 전환 박스버튼 깜빡임 회귀 제거
- **Ch3 매거진**: "엘아울이 ~ 읽은 언어" 라벨 가독성 (10.5px → 13px, uppercase 제거), "El Owl's Outside View" 섹션 전체 삭제
- **ritual "다음" 박스버튼 제거** — `buttonLabel="다음"` 은 PaginatedNarration 내장 italic 힌트 + 대화창 클릭 advance 로 처리 ("이전" 라벨과 baseline 통일)
- **버퍼링 문구**: `"잠시만요…"` → `"매거진 생성중.."` (ClosingChoiceScene + MagazineHandoffScene)
- **씬 전환 박스버튼 깜빡임 회귀 제거** — RitualScene · QuestionScene · ValueDefSingleScene · FollowupScene 의 `opacity-0` 게이트 → 조건부 마운트 + framer-motion fade-in 로 교체. 게이트 false 면 DOM 미존재 → paint timing/initial-style race 한 프레임 깜빡임 차단

## `76ebb21` — PDF 매거진 전면 리뉴얼
- 페이지 크기 **A5 → A4** (595 × 842pt)
- 모든 페이지 배경 `/paper.jpg` 풀-블리드
- Cover: cover.jpg 풀-블리드 + 동적 텍스트 (`VOL.{name}` / `- {headline} -` / 발행일) 오버레이
- EditorIntro / Chapter / EditorOutro 재구성, 매거진 톤 통일

## `f41b590` — TOC 레이아웃 + 가로선 통일 + 최소 폰트 12pt
- TOC 단독 레이아웃 분리 (Contents 큰 타이틀 + 챕터 리스트)
- 페이지 전체 가로선 굵기/색 통일 (`#59282E`)
- 최소 폰트 사이즈 12pt 가이드 (가독성 회귀 방지)

## `520e983` — TOC 동적 sub + 챕터 간격·자간 + 헤더 정리
- TOC 챕터 sub 를 LLM 이 생성한 챕터 headline 으로 동적 연결
- 챕터 항목 간격·자간 미세조정
- 페이지 헤더 정리

## `46f4b2c` — Vercel 배포 트리거
- 빈 커밋으로 dev@potenlab.dev author 우회 배포 트리거

## `1d6415f` — Ch2/Ch3 본문 wrap + TOC 챕터 간격 100
- 본문 길이가 한 페이지를 넘으면 자동 wrap (다음 페이지로 흐름)
- TOC 챕터 간격 100, 라벨 14

## `4907763` — 본문 12pt + Ch1 2-col + Ch3 deep page 상시 노출
- 본문 폰트 12pt 통일
- Ch1 본문 2-column 실험
- Ch3 deep page (pullQuote 별도 페이지) 상시 노출

## `b584094` — Ch1 본문 1-col 복귀 + Chapter hero 이미지 교체
- Ch1 2-col 가독성 회귀 → 1-col 복귀
- 챕터별 hero 이미지 교체

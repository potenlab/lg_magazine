# 2026-06-17 푸시 기록

총 17개 커밋 (`9a35996` → `92dfb35`). PDF 매거진 시안 반영 마무리 — 폰트·헤더 통일, Ch3 시안 적용, 챕터 페이지 간격·정렬 통일, 이미지 세트 (1)/(2) 랜덤 픽 도입, **신규 Appendix(Q&A 기록) 페이지 추가 및 wrap 헤더 간격 정상화**.

## `9a35996` — 직무 옵션 + C-3 CTA 박스 버튼
- 직무 선택지 옵션 확장
- C-3 (closing 직전) CTA 박스 버튼 조정

## `500a038` — PDF 폰트·헤더 위치 통일
- Noto Serif KR 등록 정비 (italic alias 포함)
- 모든 페이지 헤더 y 좌표 통일

## `9e8d2f9` — PDF 폰트·헤더·이미지 정리 + Ch3 deep page 본문 흡수
- Ch3 deep page (별도 페이지) → Ch3 main page 본문 끝에 인라인 흡수
- 폰트·헤더·이미지 미세조정

## `ec2cb7a` — MagazinePDF deep prop 잔존 제거 (빌드 픽스)
- 더 이상 사용되지 않는 `deep` prop 호출처 정리

## `c3a19af` — EditorIntro 하단 사진 / Ch1 타이틀·풀쿼트 폴리시
- EditorIntro 하단 사진 (`/intro(1).jpg`) 위치·크기 조정
- Ch1 타이틀·pullQuote 디테일 손질

## `1d3fe38` — Ch2 하단 블록 절대좌표 고정 + pullQuote 폰트 통일
- Ch2 하단 hero + pullQuote 블록 absolute bottom 46 anchor
- pullQuote 폰트 사이즈 통일

## `5b79a95` — Ch3 시안 반영 — 좌·우 타이틀 행 + 하단 hero/pullQuote 고정
- 타이틀 행: 좌 큰 타이틀(32pt) / 우 CHAPTER 3. 라벨 baseline 정렬
- 짧은 와인 룰 좌측 90pt 폭
- 부제·본문 좌측 정렬
- 하단 블록(hero + pullQuote) absolute bottom 46 anchor, top 제약 없이 콘텐츠 길이만큼 위로 자람
- pullQuote 본문 20pt + bold + 좌측 정렬, 큰 따옴표 52pt

## `0c04bb0` — 폰트·이미지 비율 정리 + Cover VOL.{name} left-anchor
- Cover `VOL.{name}` left-anchor (이름 길이에 따라 좌측으로 밀리지 않게)
- 챕터 hero 이미지 비율 정리

## `2d6af33` — PDF 헤더 Vol.{name} + magazine STORY 양쪽 표시로 통일
- 모든 본문 페이지 헤더: 좌 `Vol.{name}` / 우 `magazine STORY` + 와인 룰
- Ch1~Ch4 + EditorIntro 동일 패턴

## `d0d442f` — PDF 챕터 페이지 간격·정렬 통일
- 각 챕터 첫 콘텐츠 (#1 요소) `marginTop: 24` 통일 (Ch1 Hero / Ch2 타이틀 블록 / Ch3 타이틀 행 / Ch4 타이틀+Hero 행)
- 타이틀 ↔ 짧은 룰 ↔ 부제 간격 `20` 통일 (4 챕터 + EditorOutro 동일 리듬)
- 본문 `marginTop: 16` 통일
- Ch2 하단 블록 `alignItems: flex-end` (hero ↔ pullQuote 바닥선 정렬)
- Ch3 타이틀 행 `flex-start` (상단 정렬)
- Ch3 하단 hero + pullQuote 단일 absolute 컨테이너 (bottom 46), hero 풀폭 → `width 503 × 175`, left 0, pullQuote 위 24 gap
- Ch4 타이틀 블록 `flex-end` (hero 바닥선 정렬) + 한글 타이틀 2 줄 유지
- Cover `VOL.{name} left 370` 미세조정

## `5b49ae5` — Ch3/EditorOutro hero 사이즈 축소 + Ch2 좌측 정렬 + pullQuote 19pt
- Ch3 hero: `503×175 → 403×155` (가로 -100 / 높이 -20)
- EditorOutro hero: `height 360 → 320` (-40), 타이틀·본문 top 도 -40 보정
- Ch1·Ch2·Ch3 pullQuote 본문 폰트 `20pt → 19pt`
- Ch2 타이틀 블록 + 부제 중앙 정렬 → 좌측 정렬

## `ae23038` — 이미지 세트 (1)/(2) 랜덤 픽 + 신규 자산
- 신규 자산: `Chapter 2(2).jpg`, `Chapter 3(2).jpg`, `intro(2).jpg` (`Chapter 3(1).jpg` · `intro(1).jpg` 갱신)
- `src/lib/v3/pdf/imageSets.ts` 신규: `pickRandomVariant` + `getIntroImage` / `getChapterImage`. (1)/(2) 한 세트로 묶여 **다운로드 시점마다 한 번씩 랜덤 결정** → 한 호 안에서 EditorIntro · Ch2 · Ch3 hero 가 같은 세트로 일관
- `MagazinePDF`: `variant` 옵셔널 prop, 미전달 시 자동 픽 후 EditorIntro · Chapter 모두에 전파
- `EditorIntro` / `Chapter`: `variant` 필수 prop, src 를 helper 로 resolve
- `pdf-preview`: 도구바에 (1)/(2) 토글 추가, key 에 variant 포함
- TOC: `TITLE_TOP 100 → 80` (제목 위 여백 축소)

---

## 신규 Appendix(Q&A 기록) 페이지 — `5fafc5f` → `92dfb35`

매거진 본문 뒤에 사용자가 4개 챕터에서 주고받은 질문·답변을 모아 보여주는 **Appendix 페이지** 신설. 이후 부제·타이틀·footer 정리와 wrap(다중 페이지) 헤더 간격 정상화까지 4회 후속 폴리시.

### `5fafc5f` — PDF Appendix 페이지 추가 + 2026-06-16/17 release notes
- `src/lib/v3/pdf/pages/Appendix.tsx` 신규 (+202): Q&A thread 렌더링
- `src/lib/v3/pdf/buildAppendix.ts` 신규 (+42): 씬 데이터 → Appendix entry 변환
- `MagazinePDF`: Appendix 페이지 조립에 편입
- `ClosingChoiceScene` / `MagazineHandoffScene`: Appendix 데이터 연결
- `pdf-preview`: Appendix 탭 추가
- release notes 2026-06-16 / 2026-06-17 작성

### `4b89a06` — Appendix 부제·footer 정리 + TOC 에 APPENDIX 추가
- 부제: "네 챕터에서 주고받은 …" → "{name}님이 직접 적어주신 기록"
- 하단 footer(가로 룰 + 텍스트) 제거
- thread 간격 `marginBottom → marginTop` 전환 (wrap 시에도 동일 간격 유지)
- TOC: EDITOR'S NOTE 아래 28pt 에 **APPENDIX 항목 추가**

### `9a08339` — Appendix 타이틀·부제 정리 + wrap entry 간격 유지
- 타이틀: "내가 적은 기록" → "{name}님이 직접 적어주신 기록"
- 부제 블록 삭제 (짧은 룰 바로 아래 본문 시작)
- entry `marginBottom → marginTop` 전환 → wrap 으로 entry 가 다음 페이지 상단에 떨어져도 가로 룰 아래 호흡 유지. 챕터 첫 entry 는 `isFirst=true` 로 `marginTop 0`

### `336f548` — Appendix 헤더 marginBottom 24 (wrap 페이지 헤더 간격)
- fixed 헤더에 `marginBottom 24` → wrap 시에도 헤더 와인 룰 아래 동일 breathing room
- #1 타이틀 블록 자체 `marginTop` 제거 (헤더 marginBottom 으로 갭 확보)

### `92dfb35` — Appendix wrap 페이지 헤더 간격 정상화 (오늘 푸시)
- **근본 원인 해결**: `padding:0` Page + inner View `paddingTop` 구조에서 inner paddingTop 이 wrap 페이지에 미적용되던 문제
- Page 자체에 `paddingTop 71 / paddingHorizontal 46 / paddingBottom 50` 직접 설정 → 첫·wrap 페이지 동일 적용
- 헤더를 `fixed + absolute(top 20)` 로 전환 → flow 공간 차지 없이 모든 페이지 동일 좌표
- 좌표: top 20 + 텍스트~14 + rule mt 12 + 1 = 47, Page paddingTop 71 → 헤더 아래 24pt breathing room 일정
- 헤더 `marginBottom` · #1 `marginTop` 모두 제거 (Page paddingTop 이 갭 담당)

---

## 후속 수정 (wrap 간격·여백 폴리시)

### `d317b1c` — fix(pdf): Appendix 긴 답변 박스 페이지 경계서 분할
- `Entry` 의 `wrap={false}` 가 긴 답변을 통째로 다음 페이지로 밀어 앞 페이지 하단에 긴 여백이 생기던 문제 해결
- 질문(question)은 짧으니 `wrap=false` 유지 / 답변·결과(answer·result)는 wrap 허용 → 페이지 끝에서 중간 분할
- `minPresenceAhead: 64` — 박스가 페이지 맨 아래서 라벨만 남고 시작하지 않도록 최소 시작 공간 확보

### `dd3d4c3` — fix(pdf): Ch1~4 wrap 페이지 헤더 간격 정상화 (Appendix 동일 패턴)
- 본문이 길어 wrap 될 때 2페이지째 헤더 간격이 깨지던 문제 — `92dfb35`(Appendix) 와 동일하게 해결
- 4개 챕터 모두: Page 에 `paddingHorizontal 46 / paddingTop 71 / paddingBottom 50` 직접 부여 → inner View `paddingTop` 가 wrap 페이지 미적용되던 문제 제거
- `fixed` 헤더를 `absolute(top 20)` 로 전환 — flow 공간 차지 없이 전 페이지 동일 좌표
- inner padded View 제거, #1 요소 `marginTop 24 → 0` (Page paddingTop 이 갭 담당)
- 헤더 아래 breathing room 24pt 가 첫·wrap 페이지 모두 일정. 첫 페이지 레이아웃·하단 absolute 블록은 기존과 동일 유지

---

## 부수 인프라 (비커밋 또는 묶음 커밋 일부)
- `src/app/pdf-preview/page.tsx` — dev 라이브 PDF 프리뷰 페이지 (페이지별 탭 + (1)/(2) 토글)
- `src/proxy.ts` — dev 환경에서 `/pdf-preview` 인증 우회
- `src/lib/v3/pdf/fonts.ts` — italic alias 등록 (PDFViewer 의 italic 폴백 요청 대응)
